[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "bioinsight"
version = "0.0.1"
requires-python = ">=3.9"
dependencies = [
    "pandas",
    "numpy",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["bioinsight*"]