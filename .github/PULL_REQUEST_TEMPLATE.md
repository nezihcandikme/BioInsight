## What changed

<!-- A concise summary of the change. -->

## Why

<!-- The motivation -- a bug this fixes, a gap it closes, a linked issue. -->

## Tests performed

<!-- e.g. "Added N tests covering X, Y, Z. `pytest` passes locally
(M tests). `ruff check src/ tests/` is clean." Manual verification steps
if relevant (e.g. ran the CLI end-to-end on a real dataset). -->

## Checklist

- [ ] Tests added or updated for the change
- [ ] `pytest` passes locally
- [ ] `ruff check src/ tests/` is clean
- [ ] `CHANGELOG.md` updated under `[Unreleased]` (if user-facing)
- [ ] Docstrings updated for any changed public function signatures
